import React from 'react';
import { Text, View } from 'react-native';

// Placeholder Tab navigator stub. Replace with react-navigation tabs when integrating.
export default function TabNavigator() {
  return (
    <View style={{flex:1,alignItems:'center',justifyContent:'center'}}>
      <Text>Tab navigator placeholder. Replace with real navigator (react-navigation)</Text>
    </View>
  );
}
