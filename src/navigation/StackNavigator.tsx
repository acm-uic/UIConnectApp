import React from 'react';
import { Text, View } from 'react-native';

// Placeholder Stack navigator stub. Replace with react-navigation stack when integrating.
export default function StackNavigator() {
  return (
    <View style={{flex:1,alignItems:'center',justifyContent:'center'}}>
      <Text>Stack navigator placeholder. Replace with real navigator</Text>
    </View>
  );
}
